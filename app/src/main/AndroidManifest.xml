<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.moviedatabase.MoviesApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieDatabase">
        <activity
            android:name="com.moviedatabase.ui.HomeActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.moviedatabase.ui.SearchActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="com.moviedatabase.ui.MovieDetailActivity"
            android:exported="true">
            <intent-filter android:label="View Movie">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Dummy deeplink: moviedb://movie/{id} -->
                <data android:scheme="moviedb" android:host="movie" />
            </intent-filter>
        </activity>
        <activity android:name="com.moviedatabase.ui.SavedMoviesActivity" />
        <activity android:name="com.moviedatabase.ui.AboutActivity" />
    </application>

</manifest>